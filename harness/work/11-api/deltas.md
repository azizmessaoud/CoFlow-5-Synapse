# Deltas — 11-api

openDeltas: 0

No contract mismatch remains. Arbitrary manifests, non-green gates, bad contract hashes, and corrupt artifact bytes are rejected. Hash-bound Row 10f and Row 12 bundles are readable without copying or rewriting them.
