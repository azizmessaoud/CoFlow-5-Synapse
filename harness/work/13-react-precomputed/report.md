# Row 13 — React over precomputed evidence

The screen lists validated runs and, for the selected run, shows the controller comparison with unfinished trips, graph frames with a freshness line, decision reasons and message ids, recovery text, explanations with citation chunk ids, audit status, and limitation lines.

Every fetch goes to `/api`, which the dev server forwards to the Row 11 API. The page has no phase button, no WebSocket, and no filesystem read of artifacts. npm dependencies are pinned to exact versions. Row 14 was not started.
