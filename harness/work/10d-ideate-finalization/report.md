# Row 10d report — Ideate finalization

## Verdict

The seven-person, persona-led agentic-AI Ideate addendum and its professor-ready two-minute view are complete and machine-validated.

## Delivered

- Source of truth: `.scratch/coflow5-ideate-finalize/spec.md`.
- 56 decision-matrix ideas: exactly eight each for P1 Amara, P2 David, P3 Chidi, P4 Rosa, P5 Marcus, P6 Yuki, and P7 Omar.
- Seven chosen Now ideas with a directly derived A.1 presentation view.
- Standalone Omar/A4 anomaly track with transparent detector ladder, SCAMPER, morphology, explicit vision states, alert contract, source classes, heatmaps, and Synapse limits.
- Ten data classes separated into prototype-safe Now inputs and unbuilt Later connectors, with provenance, licence, freshness, uncertainty, and source-type labels.
- 24 future problems across eight clusters, each naming first-affected persona, mitigation owner, architecture stress, and Ideate seed.
- Final architecture preserves A1 sole actuation, cooperative Max-Pressure, deterministic safety and recovery, advisory A2–A5, and non-actuating bounded Synapse workers.

## Professor-ready follow-up

A.1 now uses the supplied five-column spoken form: `Persona | Say this | Agent | Metric | First failure`. The acceptance test compares all seven lines verbatim, verifies their source IDs are the seven chosen Now matrix rows, and requires the explicit close: Omar’s highest-risk failure is presenting a network we cannot see as a healthy network.

## Honesty, roster, and authority

The roster is seven. Yuki is P6. Maria and Leila are not idea owners or personas. I-7 remains an optional A5 system KPI without a persona. A1 still writes every signal, and the explanation page never reaches the simulator. The addendum forbids lives-saved inference, measured-air-quality claims from HBEFA/SUMO, scrape-as-truth, scheduled GTFS as live positions, weather as causal crash proof, accident-prevention claims from simulation, and best-episode headlines.

## Validation

```text
py -3.11 -m pytest tests/acceptance/test_ideate_finalization.py -q
.....                                                                    [100%]
5 passed in 0.39s
```

The validator parses the Markdown source rather than self-scoring prose. It checks exact A.1 wording, source-row mapping, matrix schemas and counts, IDs, persona ownership, fit/disposition invariants, Omar fields, data classes, future-risk clusters, authority boundaries, and claim flags.

## Highest-risk future problem

Omar/A4 can fail safely only if missing, stale, invalid, or poisoned data becomes `stale`, `degraded`, or `insufficient_data`—never a healthy-looking `normal` state.
